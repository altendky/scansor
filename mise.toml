[settings]
locked = true

[tools]
node = "25.8.2"
python = ["3.12.14", "3.13.15"]
uv = "0.11.2"
"pipx:pre-commit" = "4.6.1"
"cargo:lychee" = "0.24.2"

[tasks.release]
description = "Prepare a signed release version PR: mise run release X.Y.Z"
run = "uv run scripts/release-version.py prepare"
